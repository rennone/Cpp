/*
char* m_strcat(const char* a, const char *b)
{
  char hello[256];
  int i=0;
  while(a[i] != '\n')
  {
    hello[i] = a[i];
    i++;
  }
  int j=0;
  while(b[j] != '\0')
  {
    hello[i+j] = b[j];
    j++;
  }
  return hello;
}
*/
char* getChar()
{
  char *hello;
  return hello;
}

#include <stdio.h>
int main()
{
  printf("%s\n", getChar());
  return 0;
}
